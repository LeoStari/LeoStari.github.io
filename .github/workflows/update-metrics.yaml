name: update-metrics

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # fetch-depth: 0 is usually not needed for auto-commit, but kept for safety
          fetch-depth: 0 

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run fetch script
        run: |
          python .github/scripts/fetch_metrics.py

      - name: Commit & push metrics
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update research metrics"
          file_pattern: "assets/data/metrics.json"
          # This creates a branch update if main has moved forward
          branch: ${{ github.ref }}